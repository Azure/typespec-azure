name: Benchmark External

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      specs:
        description: "Optional comma-separated service filter"
        type: string
        default: ""
      runner:
        description: "Runner label (for stable runs prefer self-hosted or larger dedicated runner)"
        required: false
        type: string
        default: "ubuntu-latest"

permissions:
  contents: write

concurrency:
  group: benchmark-external-${{ github.ref }}-${{ inputs.specs }}
  cancel-in-progress: true

jobs:
  run:
    uses: ./.github/workflows/benchmark-run.yml
    with:
      specs-dir: packages/benchmark/external-spec
      results-dir: external-results
      iterations: 25
      warmup: 3
      emitter-iterations: 3
      emitter-warmup: 1
      specs: ${{ inputs.specs || '' }}
      noise-cv: 0.1
      rerun-iterations: 2
      runner: ${{ github.event_name == 'workflow_dispatch' && inputs.runner || vars.BENCHMARK_RUNNER || 'ubuntu-latest' }}
    permissions:
      contents: write
    secrets: inherit
