// The experimental base-types library is TypeSpec-only (it defines models and a
// versioned namespace but no decorators), so there is nothing to export at runtime.
// This module exists solely to provide the JS entrypoint that the playground bundler
// requires for the "./base-types" package export.
export {};
