# Query parameters whose names start with characters that are invalid in Go identifiers are cased correctly

## TypeSpec

```tsp
@service(#{ title: "Regressions" })
namespace Regressions;

@post
@route("/fixed-param-name")
op withExpandParam(@query("$expand") $expand: string, @query("$top") $top?: int32): void;
```

## The generated client exposes `Expand` and `Top` parameters while wiring the `$`-prefixed query names

```go client
// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
// Code generated by Microsoft (R) Go Code Generator. DO NOT EDIT.

package testmodule

import (
	"context"
	"github.com/Azure/azure-sdk-for-go/sdk/azcore"
	"github.com/Azure/azure-sdk-for-go/sdk/azcore/policy"
	"github.com/Azure/azure-sdk-for-go/sdk/azcore/runtime"
	"net/http"
	"strconv"
	"strings"
)

// RegressionsClient contains the methods for the Regressions group.
// Don't use this type directly, use NewRegressionsClientWithNoCredential() instead.
type RegressionsClient struct {
	internal *azcore.Client
	endpoint string
}

// RegressionsClientOptions contains the optional values for creating a [RegressionsClient].
type RegressionsClientOptions struct {
	azcore.ClientOptions
}

// NewRegressionsClientWithNoCredential creates a new instance of RegressionsClient with the specified values.
//   - endpoint - Service host
//   - options - Contains optional client configuration. Pass nil to accept the default values.
func NewRegressionsClientWithNoCredential(endpoint string, options *RegressionsClientOptions) (*RegressionsClient, error) {
	if options == nil {
		options = &RegressionsClientOptions{}
	}
	cl, err := azcore.NewClient(moduleName, moduleVersion, runtime.PipelineOptions{}, &options.ClientOptions)
	if err != nil {
		return nil, err
	}
	client := &RegressionsClient{
		endpoint: endpoint,
		internal: cl,
	}
	return client, nil
}

// WithExpandParam -
// If the operation fails it returns an *azcore.ResponseError type.
//   - options - RegressionsClientWithExpandParamOptions contains the optional parameters for the RegressionsClient.WithExpandParam
//     method.
func (client *RegressionsClient) WithExpandParam(ctx context.Context, expand string, options *RegressionsClientWithExpandParamOptions) (RegressionsClientWithExpandParamResponse, error) {
	var err error
	req, err := client.withExpandParamCreateRequest(ctx, expand, options)
	if err != nil {
		return RegressionsClientWithExpandParamResponse{}, err
	}
	httpResp, err := client.internal.Pipeline().Do(req)
	if err != nil {
		return RegressionsClientWithExpandParamResponse{}, err
	}
	if !runtime.HasStatusCode(httpResp, http.StatusNoContent) {
		err = runtime.NewResponseError(httpResp)
		return RegressionsClientWithExpandParamResponse{}, err
	}
	return RegressionsClientWithExpandParamResponse{}, nil
}

// withExpandParamCreateRequest creates the WithExpandParam request.
func (client *RegressionsClient) withExpandParamCreateRequest(ctx context.Context, expand string, options *RegressionsClientWithExpandParamOptions) (*policy.Request, error) {
	urlPath := "/fixed-param-name"
	req, err := runtime.NewRequest(ctx, http.MethodPost, runtime.JoinPaths(client.endpoint, urlPath))
	if err != nil {
		return nil, err
	}
	reqQP := req.Raw().URL.Query()
	reqQP.Set("$expand", expand)
	if options != nil && options.Top != nil {
		reqQP.Set("$top", strconv.FormatInt(int64(*options.Top), 10))
	}
	req.Raw().URL.RawQuery = strings.ReplaceAll(reqQP.Encode(), "+", "%20")
	return req, nil
}

```
