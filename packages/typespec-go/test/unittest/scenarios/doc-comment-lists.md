# Bullet and numbered lists in doc comments are rendered per the Go doc convention

## TypeSpec

```tsp
@service(#{ title: "DocLists" })
namespace DocLists;

/**
 * This tests:
 * - Simple bullet point. This bullet point is going to be very long to test how text wrapping is handled in bullet points within documentation comments. It should properly indent the wrapped lines.
 * - Another bullet point with bold text.
 * - Third bullet point with italic text.
 *
 * Steps to follow:
 * 1. First step with important note.
 * 2. Second step with emphasis.
 * 3. Third step.
 */
model BulletPointsModel {
  /**
   * A property whose documentation contains a list:
   * - one
   * - two
   */
  prop: string;
}

/**
 * - A doc comment that is nothing but a list, with no prose line anywhere.
 * - Go strips the common indentation of every line before looking for lists, so an indented list here would be demoted to a paragraph.
 * - The "PureListModel -" prefix moves onto its own line, restoring a prose line that keeps the list renderable.
 */
model PureListModel {
  /**
   * - A required property whose documentation is nothing but a list.
   * - The "REQUIRED" modifier moves onto its own line so this item isn't absorbed into it.
   */
  required: string;

  /**
   * - An optional property whose documentation is nothing but a list.
   * - Nothing supplies a prose line here, so this falls back to plain wrapped prose.
   */
  optional?: string;
}

/**
 * - An enum whose doc comment is nothing but a list.
 * - The "PureListEnum -" prefix moves onto its own line so this item isn't absorbed into it.
 */
union PureListEnum {
  string,

  /**
   * - An enum value whose doc comment is nothing but a list.
   * - Second item.
   */
  One: "one",
}

@@access(DocLists.BulletPointsModel, Access.public);
@@usage(DocLists.BulletPointsModel, Usage.output);
@@access(DocLists.PureListModel, Access.public);
@@usage(DocLists.PureListModel, Usage.output);
@@access(DocLists.PureListEnum, Access.public);
@@usage(DocLists.PureListEnum, Usage.output);
```

## The generated model

```go models
// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
// Code generated by Microsoft (R) Go Code Generator. DO NOT EDIT.

package testmodule

// BulletPointsModel - This tests:
//
//   - Simple bullet point. This bullet point is going to be very long to test how text wrapping is handled in bullet points within
//     documentation comments. It should properly indent the wrapped lines.
//   - Another bullet point with bold text.
//   - Third bullet point with italic text.
//
// Steps to follow:
//
//  1. First step with important note.
//  2. Second step with emphasis.
//  3. Third step.
type BulletPointsModel struct {
	// REQUIRED; A property whose documentation contains a list:
	//
	//   - one
	//   - two
	Prop *string
}

// PureListModel -
//
//   - A doc comment that is nothing but a list, with no prose line anywhere.
//   - Go strips the common indentation of every line before looking for lists, so an indented list here would be demoted to a
//     paragraph.
//   - The "PureListModel -" prefix moves onto its own line, restoring a prose line that keeps the list renderable.
type PureListModel struct {
	// REQUIRED
	//
	//   - A required property whose documentation is nothing but a list.
	//   - The "REQUIRED" modifier moves onto its own line so this item isn't absorbed into it.
	Required *string

	// - An optional property whose documentation is nothing but a list.
	// - Nothing supplies a prose line here, so this falls back to plain wrapped prose.
	Optional *string
}
```

## The generated constants

```go constants
// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
// Code generated by Microsoft (R) Go Code Generator. DO NOT EDIT.

package testmodule

// PureListEnum -
//
//   - An enum whose doc comment is nothing but a list.
//   - The "PureListEnum -" prefix moves onto its own line so this item isn't absorbed into it.
type PureListEnum string

const (
	// PureListEnumOne -
	//
	//   - An enum value whose doc comment is nothing but a list.
	//   - Second item.
	PureListEnumOne PureListEnum = "one"
)

// PossiblePureListEnumValues returns the possible values for the PureListEnum const type.
func PossiblePureListEnumValues() []PureListEnum {
	return []PureListEnum{
		PureListEnumOne,
	}
}
```
