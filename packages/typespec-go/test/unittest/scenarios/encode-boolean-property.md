# A boolean property encoded as a string uses string JSON serde

## TypeSpec

```tsp
@service(#{ title: "EncodeBooleanProperty" })
namespace EncodeBooleanProperty;

model BoolAsStringProperty {
  @encode(string)
  value: boolean;
}

@post
@route("/bool")
op send(@body value: BoolAsStringProperty): BoolAsStringProperty;
```

## The generated model serde

```go models_serde
// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
// Code generated by Microsoft (R) Go Code Generator. DO NOT EDIT.

package testmodule

import (
	"encoding/json"
	"fmt"
	"github.com/Azure/azure-sdk-for-go/sdk/azcore"
	"github.com/Azure/azure-sdk-for-go/sdk/azcore/to"
	"strconv"
	"strings"
)

// MarshalJSON implements the json.Marshaller interface for type BoolAsStringProperty.
func (b BoolAsStringProperty) MarshalJSON() ([]byte, error) {
	objectMap := make(map[string]any)
	if b.Value != nil {
		if azcore.IsNullValue(b.Value) {
			objectMap["value"] = nil
		} else {
			objectMap["value"] = strconv.FormatBool(*b.Value)
		}
	}
	return json.Marshal(objectMap)
}

// UnmarshalJSON implements the json.Unmarshaller interface for type BoolAsStringProperty.
func (b *BoolAsStringProperty) UnmarshalJSON(data []byte) error {
	var rawMsg map[string]json.RawMessage
	if err := json.Unmarshal(data, &rawMsg); err != nil {
		return fmt.Errorf("unmarshalling type %T: %s", b, err.Error())
	}
	for key, val := range rawMsg {
		var err error
		switch key {
		case "value":
			var aux string
			err = unpopulate(val, "Value", &aux)
			if err == nil {
				var v bool
				v, err = strconv.ParseBool(strings.ToLower(aux))
				if err == nil {
					b.Value = to.Ptr(v)
				}
			}
			delete(rawMsg, key)
		}
		if err != nil {
			return fmt.Errorf("unmarshalling type %T: %s", b, err.Error())
		}
	}
	return nil
}

func unpopulate(data json.RawMessage, fn string, v any) error {
	if data == nil || string(data) == "null" {
		return nil
	}
	if err := json.Unmarshal(data, v); err != nil {
		return fmt.Errorf("struct field %s: %s", fn, err.Error())
	}
	return nil
}
```
