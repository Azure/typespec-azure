# Typespec Azure Samples

```bash
pnpm test    # Check Samples match snapshots
pnpm test:ci # run test same as CI

pnpm test:regen -- -g "<sample-name>" # Regen of this name

pnpm test:regen # Regen all samples.
```
