This diagnostic is issued when TCGC cannot determine the emitter name from its options. The emitter name is needed to resolve language-specific scopes and naming rules.

To fix this issue, configure the `emitter-name` option for the emitter invoking TCGC.
