{
  "name": "@azure-tools/spector-runner",
  "version": "0.1.0",
  "private": true,
  "description": "Shared loader and schema for the standardized per-emitter spector test selection config (spector.config.yaml).",
  "type": "module",
  "license": "MIT",
  "author": "Microsoft Corporation",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Azure/typespec-azure.git"
  },
  "bugs": {
    "url": "https://github.com/Azure/typespec-azure/issues"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/src/index.js"
    },
    "./schema": "./schema/dist/SpectorConfig.json"
  },
  "files": [
    "dist/**",
    "!dist/test/**",
    "schema/dist/SpectorConfig.json"
  ],
  "scripts": {
    "build": "pnpm regen-schema && tsc -p tsconfig.build.json",
    "regen-schema": "tsp compile ./schema/spector-config.tsp --config ./schema/tspconfig.yaml --warn-as-error",
    "watch": "tsc -p tsconfig.build.json --watch",
    "clean": "rimraf ./dist ./schema/dist ./temp",
    "lint": "oxlint . --deny-warnings",
    "lint:fix": "oxlint . --fix",
    "test": "vitest run",
    "test:watch": "vitest -w",
    "test:ci": "vitest run --coverage --reporter=junit --reporter=default"
  },
  "dependencies": {
    "yaml": "catalog:"
  },
  "devDependencies": {
    "@typespec/compiler": "workspace:^",
    "@typespec/json-schema": "workspace:^",
    "rimraf": "catalog:",
    "vitest": "catalog:"
  }
}
