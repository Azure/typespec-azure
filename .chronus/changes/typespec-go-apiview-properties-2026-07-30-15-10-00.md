---
changeKind: feature
packages:
  - "@azure-tools/typespec-go"
---

Emit `testdata/apiview-properties.json` mapping the Go APIView line IDs of the generated models, enums, enum values, clients, constructors and methods to their TCGC cross-language definition IDs, so APIView can link the Go API surface to the other languages.
