---
changeKind: feature
packages:
  - "@azure-tools/typespec-client-generator-core"
---

Add `convenient-api-requires-scope` diagnostic warning when `@convenientAPI` is used without a language scope.
