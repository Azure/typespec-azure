---
changeKind: feature
packages:
  - "@azure-tools/typespec-client-generator-core"
  - "@azure-tools/typespec-azure-rulesets"
---

Add the `use-model-inheritance` linter rule to require a nominal model hierarchy for model variants of `union extends`, enabled by the `client-sdk` ruleset.
