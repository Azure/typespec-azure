---
changeKind: feature
packages:
  - "@azure-tools/typespec-ts"
---

Add an experimental option to generate visibility-specific request models that exclude lifecycle-invisible properties while preserving HTTP metadata properties in the client model.