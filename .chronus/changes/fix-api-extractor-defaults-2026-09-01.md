---
changeKind: internal
packages:
  - "@azure-tools/typespec-ts"
---

Handle api-extractor >=7.59.0 moving `_defaultConfig` to a module-level constant by falling back to loading the defaults JSONC file directly.
