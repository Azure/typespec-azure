---
changeKind: internal
packages:
  - "@azure-tools/typespec-azure-core"
---

Add missing `@doc` on the `location` property of `LocationOfCreatedResourceResponse` and `LocationOfCreatedResourceWithServiceProvidedNameResponse` in `foundations.tsp`, required by the compiler's `library-linter` `missing-documentation` rule.
