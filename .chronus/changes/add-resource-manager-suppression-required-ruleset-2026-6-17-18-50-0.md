---
changeKind: feature
packages:
  - "@azure-tools/typespec-azure-rulesets"
---

Add new `resource-manager-suppression-required` ruleset based on the `resource-manager` ruleset with select rules disabled that require explicit suppression.
