---
changeKind: feature
packages:
  - "@azure-tools/typespec-azure-resource-manager"
  - "@azure-tools/typespec-azure-rulesets"
---

Add the `no-uuid` ARM lint rule, migrated from the Swagger `GuidUsage` validator rule and disabled by default in the resource-manager ruleset.
