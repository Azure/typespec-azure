---
changeKind: feature
packages:
  - "@azure-tools/azure-http-specs"
---

Add spector scenario for relative nextLink in pagination to test Azure services that use relative URLs instead of absolute URLs for pagination nextLink.