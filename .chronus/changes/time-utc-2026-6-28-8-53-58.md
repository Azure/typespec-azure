---
changeKind: fix
packages:
  - "@azure-tools/typespec-go"
---

Ensure UTC date-time values are converted to UTC before sending.  Update example generation for date-time values to use time.Date() instead of time.Parse() which leaks the wire format.